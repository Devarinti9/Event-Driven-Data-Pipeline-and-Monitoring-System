version: "3.9"

services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: pipeline_db
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5436:5432"
    volumes:
      - pipeline_postgres:/var/lib/postgresql/data

  api:
    build: .
    depends_on:
      - postgres
    env_file:
      - .env.example
    ports:
      - "8003:8000"
    volumes:
      - ./app:/app/app

  airflow:
    image: apache/airflow:2.10.2
    depends_on:
      - api
    environment:
      AIRFLOW__CORE__LOAD_EXAMPLES: "false"
    command: bash -c "airflow db migrate && airflow standalone"
    volumes:
      - ./dags:/opt/airflow/dags
    ports:
      - "8081:8080"

volumes:
  pipeline_postgres:
